name: Nightly Build
run-name: Nightly build
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

# 全局环境变量（抽离链接中的固定部分）
env:
  REPO_OWNER: xiaoyi1972          # 仓库所有者
  REPO_NAME: ubuntu-rockchip      # 仓库名称
  RELEASE_TAG: workflow           # Release 标签
  ROOTFS_FILE_PREFIX: ubuntu      # rootfs 文件前缀
  ROOTFS_ARCH: arm64              # 架构

jobs:
  config:
    runs-on: ubuntu-22.04
    name: Create martrix
    outputs:
      build_matrix: ${{ steps.build.outputs.build_matrix }}
      rootfs_matrix: ${{ steps.rootfs.outputs.rootfs_matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create build matrix
        id: build
        run: |
            build_matrix=""
            for file in config/boards/*; do
                COMPATIBLE_SUITES=()
                COMPATIBLE_FLAVORS=()
                # shellcheck disable=SC1090
                source "${file}"
                for suite in "${COMPATIBLE_SUITES[@]}"; do
                    for flavor in "${COMPATIBLE_FLAVORS[@]}"; do
                    build_matrix+="{\"board\":\"$(basename "${file%.sh}")\",\"suite\":\"${suite}\",\"flavor\":\"${flavor}\"},"
                    done
                done
            done
            echo "build_matrix={\"include\":[${build_matrix::-1}]}" >> $GITHUB_OUTPUT

      - name: Create rootfs matrix
        id: rootfs
        run: |
            rootfs_matrix=""
            for suite in config/suites/*; do
                for flavor in config/flavors/*; do
                    rootfs_matrix+="{\"suite\":\"$(basename "${suite%.sh}")\",\"flavor\":\"$(basename "${flavor%.sh}")\"},"
                done
            done
            echo "rootfs_matrix={\"include\":[${rootfs_matrix::-1}]}" >> $GITHUB_OUTPUT

  rootfs:
    runs-on: ubuntu-22.04
    name: Build rootfs 
    needs: [config]
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.rootfs_matrix) }}
    steps:
      - name: Get more disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug - Check current directory and files
        run: |
          pwd
          ls -la
          ls -la config/suites/

      - name: Set environment variables
        id: vars
        run: |
          source config/suites/${{ matrix.suite }}.sh
          echo "suite_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT
          # 拼接目标 rootfs 文件名（zip 包）
          ROOTFS_ZIP_NAME="${{ env.ROOTFS_FILE_PREFIX }}-${RELEASE_VERSION}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-rootfs.zip"
          # 拼接目标 rootfs 解压后的文件名（tar.xz）
          ROOTFS_TAR_NAME="${{ env.ROOTFS_FILE_PREFIX }}-${RELEASE_VERSION}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}.rootfs.tar.xz"
          # 拼接远程下载链接
          ROOTFS_DOWNLOAD_URL="https://github.com/${{ env.REPO_OWNER }}/${{ env.REPO_NAME }}/releases/download/${{ env.RELEASE_TAG }}/${ROOTFS_ZIP_NAME}"
          
          echo "rootfs_zip_name=$ROOTFS_ZIP_NAME" >> $GITHUB_OUTPUT
          echo "rootfs_tar_name=$ROOTFS_TAR_NAME" >> $GITHUB_OUTPUT
          echo "rootfs_download_url=$ROOTFS_DOWNLOAD_URL" >> $GITHUB_OUTPUT

      - name: Install GitHub CLI (if missing)
        run: |
          if ! command -v gh &> /dev/null; then
            sudo apt update && sudo apt install -y gh
          fi
          # 验证 gh 版本
          gh --version

      - name: Authenticate GitHub CLI (用于检查 Release 资产)
        run: echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token

      - name: Check if rootfs zip exists in remote Release
        id: check_remote_rootfs
        run: |
          # 检查指定 Release 中是否存在目标 zip 包
          gh release view ${{ env.RELEASE_TAG }} \
            --repo ${{ env.REPO_OWNER }}/${{ env.REPO_NAME }} \
            --json assets \
            --jq ".assets[] | select(.name == \"${{ steps.vars.outputs.rootfs_zip_name }}\") | .name" > /tmp/asset_check.txt
          
          if [ -s /tmp/asset_check.txt ]; then
            echo "✅ Found remote rootfs zip: ${{ steps.vars.outputs.rootfs_zip_name }}"
            echo "remote_exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Remote rootfs zip not found: ${{ steps.vars.outputs.rootfs_zip_name }}"
            echo "remote_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Download remote rootfs zip (if exists)
        if: steps.check_remote_rootfs.outputs.remote_exists == 'true'
        run: |
          # 创建 build 目录（确保目录存在）
          mkdir -p ${{ github.workspace }}/build
          # 下载远程 zip 包到 build 目录
          wget -O ${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_zip_name }} ${{ steps.vars.outputs.rootfs_download_url }}
          # 验证下载文件
          ls -la ${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_zip_name }}
          # 解压 zip 包（提取里面的 tar.xz 文件）
          unzip -o ${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_zip_name }} -d ${{ github.workspace }}/build/
          # 验证解压后的 tar.xz 文件
          ls -la ${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_tar_name }}

      - name: Install dependencies (only if remote rootfs not found)
        if: steps.check_remote_rootfs.outputs.remote_exists == 'false'
        shell: bash
        run: |
          sudo apt-get update && sudo apt-get upgrade -y
          sudo apt-get install -y build-essential gcc-aarch64-linux-gnu bison \
          qemu-user-static qemu-system-arm qemu-efi u-boot-tools binfmt-support \
          debootstrap flex libssl-dev bc rsync kmod cpio xz-utils fakeroot parted \
          udev dosfstools uuid-runtime git-lfs device-tree-compiler python2 python3 \
          python-is-python3 fdisk bc debhelper python3-pyelftools python3-setuptools \
          python3-distutils python3-pkg-resources swig libfdt-dev libpython3-dev dctrl-tools
      
      - name: Add execute permission to scripts (only if remote rootfs not found)
        if: steps.check_remote_rootfs.outputs.remote_exists == 'false'
        run: |
          chmod +x ./build.sh
          chmod +x ./scripts/*.sh
          ls -la ./build.sh
          ls -la ./scripts/build-rootfs.sh

      - name: Build rootfs (only if remote rootfs not found)
        if: steps.check_remote_rootfs.outputs.remote_exists == 'false'
        shell: bash
        run: |
          # sudo $GITHUB_WORKSPACE/build.sh --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }} --rootfs-only --launchpad
          sudo $GITHUB_WORKSPACE/build.sh --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }} --rootfs-only

      - name: Debug - Check rootfs file
        run: |
          TARGET_FILE="${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_tar_name }}"
          if [ -f "$TARGET_FILE" ]; then
            echo "✅ Rootfs file exists: $TARGET_FILE"
            du -h "$TARGET_FILE"
          else
            echo "❌ Rootfs file not found: $TARGET_FILE"
            exit 1
          fi

      - name: Upload rootfs artifact
        uses: actions/upload-artifact@v4.3.3
        with:
            name: ${{ env.ROOTFS_FILE_PREFIX }}-${{ steps.vars.outputs.suite_version }}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-rootfs
            path: ${{ github.workspace }}/build/${{ steps.vars.outputs.rootfs_tar_name }}
            if-no-files-found: error
            compression-level: 6
            overwrite: false

  build:
    runs-on: ubuntu-22.04
    needs: [rootfs, config]
    name: Build image 
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.build_matrix) }}
    steps:
      - name: Get more disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug - Check current directory and files
        run: |
          pwd
          ls -la
          ls -la config/boards/

      - name: Set environment variables
        id: vars
        run: |
          source config/suites/${{ matrix.suite }}.sh
          echo "suite_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT

      - name: Checkout rootfs
        uses: actions/download-artifact@v4.1.2
        with:
            name: ${{ env.ROOTFS_FILE_PREFIX }}-${{ steps.vars.outputs.suite_version }}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-rootfs
            path: ${{ github.workspace }}/build/

      - name: Debug - Check downloaded rootfs
        run: |
          echo "=== Downloaded rootfs files ==="
          ls -la ${{ github.workspace }}/build/

      - name: Install dependencies
        shell: bash
        run: |
          sudo apt-get update && sudo apt-get upgrade -y
          sudo apt-get install -y build-essential gcc-aarch64-linux-gnu bison \
          qemu-user-static qemu-system-arm qemu-efi u-boot-tools binfmt-support \
          debootstrap flex libssl-dev bc rsync kmod cpio xz-utils fakeroot parted \
          udev dosfstools uuid-runtime git-lfs device-tree-compiler python2 python3 \
          python-is-python3 fdisk bc debhelper python3-pyelftools python3-setuptools \
          python3-distutils python3-pkg-resources swig libfdt-dev libpython3-dev dctrl-tools

      - name: Add execute permission to build.sh
        run: |
          chmod +x ./build.sh
          ls -la ./build.sh

      - name: Build image
        shell: bash
        run: |
          # sudo $GITHUB_WORKSPACE/build.sh --board=${{ matrix.board }} --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }} --launchpad
          sudo $GITHUB_WORKSPACE/build.sh --board=${{ matrix.board }} --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }}

      - name: Debug - Check image file generation
        run: |
          echo "=== Images directory contents ==="
          ls -la ${{ github.workspace }}/images/

      - name: Upload image artifact
        uses: actions/upload-artifact@v4.3.3
        with:
          name: ${{ env.ROOTFS_FILE_PREFIX }}-${{ steps.vars.outputs.suite_version }}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}
          path: ${{ github.workspace }}/images/${{ env.ROOTFS_FILE_PREFIX }}-*-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}.*
          if-no-files-found: error
          compression-level: 6
          overwrite: false

      - name: Clean cache
        shell: bash
        run: sync && sudo rm -rf ${{ github.workspace }}/images/ ${{ github.workspace }}/build/ && sync
