name: Nightly Build
run-name: Nightly build
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

env:
  REPO_OWNER: xiaoyi1972
  REPO_NAME: ubuntu-rockchip
  RELEASE_TAG: workflow
  ROOTFS_FILE_PREFIX: ubuntu
  ROOTFS_ARCH: arm64

jobs:
  config:
    runs-on: ubuntu-22.04
    name: Create martrix
    outputs:
      build_matrix: ${{ steps.build.outputs.build_matrix }}
      rootfs_matrix: ${{ steps.rootfs.outputs.rootfs_matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create build matrix
        id: build
        run: |
            build_matrix=""
            for file in config/boards/*; do
                COMPATIBLE_SUITES=()
                COMPATIBLE_FLAVORS=()
                # shellcheck disable=SC1090
                source "${file}"
                for suite in "${COMPATIBLE_SUITES[@]}"; do
                    for flavor in "${COMPATIBLE_FLAVORS[@]}"; do
                    build_matrix+="{\"board\":\"$(basename "${file%.sh}")\",\"suite\":\"${suite}\",\"flavor\":\"${flavor}\"},"
                    done
                done
            done
            echo "build_matrix={\"include\":[${build_matrix::-1}]}" >> $GITHUB_OUTPUT

      - name: Create rootfs matrix
        id: rootfs
        run: |
            rootfs_matrix=""
            for suite in config/suites/*; do
                for flavor in config/flavors/*; do
                    rootfs_matrix+="{\"suite\":\"$(basename "${suite%.sh}")\",\"flavor\":\"$(basename "${flavor%.sh}")\"},"
                done
            done
            echo "rootfs_matrix={\"include\":[${rootfs_matrix::-1}]}" >> $GITHUB_OUTPUT

  rootfs:
    runs-on: ubuntu-22.04
    name: Build rootfs 
    needs: [config]
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.rootfs_matrix) }}
    steps:
      # ...下略，原样保留...

  build:
    runs-on: ubuntu-22.04
    needs: [rootfs, config]
    name: Build image 
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.build_matrix) }}
    steps:
      - name: Get more disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug - Check current directory and files
        run: |
          pwd
          ls -la
          ls -la config/boards/

      - name: Set environment variables
        id: vars
        run: |
          source config/suites/${{ matrix.suite }}.sh
          echo "suite_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT

      - name: Set DEB Asset Name
        id: deb-asset
        run: |
          DEB_ZIP_NAME="deb-packages-${{ matrix.board }}-${{ matrix.suite }}.zip"
          DEB_DOWNLOAD_URL="https://github.com/${{ env.REPO_OWNER }}/${{ env.REPO_NAME }}/releases/download/${{ env.RELEASE_TAG }}/${DEB_ZIP_NAME}"
          echo "deb_zip_name=$DEB_ZIP_NAME" >> $GITHUB_OUTPUT
          echo "deb_download_url=$DEB_DOWNLOAD_URL" >> $GITHUB_OUTPUT

      - name: Install GitHub CLI (if missing)
        run: |
          if ! command -v gh &> /dev/null; then
            sudo apt update && sudo apt install -y gh
          fi
          gh --version

      - name: Authenticate GitHub CLI
        run: echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token

      - name: Check if DEB ZIP exists in remote Release
        id: check_remote_deb
        run: |
          gh release view ${{ env.RELEASE_TAG }} \
            --repo ${{ env.REPO_OWNER }}/${{ env.REPO_NAME }} \
            --json assets \
            --jq ".assets[] | select(.name == \"${{ steps.deb-asset.outputs.deb_zip_name }}\") | .name" > /tmp/deb_asset_check.txt
          if [ -s /tmp/deb_asset_check.txt ]; then
            echo "✅ Found remote deb zip: ${{ steps.deb-asset.outputs.deb_zip_name }}"
            echo "deb_remote_exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Remote deb zip not found: ${{ steps.deb-asset.outputs.deb_zip_name }}"
            echo "deb_remote_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Download remote DEB zip (if exists)
        if: steps.check_remote_deb.outputs.deb_remote_exists == 'true'
        run: |
          mkdir -p ${{ github.workspace }}/build
          wget -O ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }} ${{ steps.deb-asset.outputs.deb_download_url }}
          ls -la ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }}
          unzip -o ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }} -d ${{ github.workspace }}/build/
          ls -la ${{ github.workspace }}/build/*.deb

      - name: Install required build dependencies
        if: steps.check_remote_deb.outputs.deb_remote_exists != 'true'
        shell: bash
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential device-tree-compiler u-boot-tools

      - name: Ensure images, build, chroot, and rootfs directories exist   # ← 新增和改进
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/images
          mkdir -p ${{ github.workspace }}/build
          mkdir -p chroot/etc/default         # 保证chroot相关目录存在
          mkdir -p rootfs/dev                 # 保证rootfs相关目录存在

      - name: Build image 并忽略DTS相关错误
        shell: bash
        run: |
          set +e
          sudo $GITHUB_WORKSPACE/build.sh --board=${{ matrix.board }} --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }}
          true
        continue-on-error: true

      - name: Ensure images directory exists (pre-upload/debug)
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/images

      - name: Debug - Check image file generation
        run: |
          echo "=== Images directory contents ==="
          ls -la ${{ github.workspace }}/images/

      - name: Upload image artifact
        uses: actions/upload-artifact@v4.3.3
        with:
          name: ${{ env.ROOTFS_FILE_PREFIX }}-${{ steps.vars.outputs.suite_version }}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}
          path: ${{ github.workspace }}/images/${{ env.ROOTFS_FILE_PREFIX }}-*-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}.*
          if-no-files-found: error
          compression-level: 6
          overwrite: false

      - name: Clean cache
        shell: bash
        run: sync && sudo rm -rf ${{ github.workspace }}/images/ ${{ github.workspace }}/build/ && syncname: Nightly Build
run-name: Nightly build
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

env:
  REPO_OWNER: xiaoyi1972
  REPO_NAME: ubuntu-rockchip
  RELEASE_TAG: workflow
  ROOTFS_FILE_PREFIX: ubuntu
  ROOTFS_ARCH: arm64

jobs:
  config:
    runs-on: ubuntu-22.04
    name: Create martrix
    outputs:
      build_matrix: ${{ steps.build.outputs.build_matrix }}
      rootfs_matrix: ${{ steps.rootfs.outputs.rootfs_matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create build matrix
        id: build
        run: |
            build_matrix=""
            for file in config/boards/*; do
                COMPATIBLE_SUITES=()
                COMPATIBLE_FLAVORS=()
                # shellcheck disable=SC1090
                source "${file}"
                for suite in "${COMPATIBLE_SUITES[@]}"; do
                    for flavor in "${COMPATIBLE_FLAVORS[@]}"; do
                    build_matrix+="{\"board\":\"$(basename "${file%.sh}")\",\"suite\":\"${suite}\",\"flavor\":\"${flavor}\"},"
                    done
                done
            done
            echo "build_matrix={\"include\":[${build_matrix::-1}]}" >> $GITHUB_OUTPUT

      - name: Create rootfs matrix
        id: rootfs
        run: |
            rootfs_matrix=""
            for suite in config/suites/*; do
                for flavor in config/flavors/*; do
                    rootfs_matrix+="{\"suite\":\"$(basename "${suite%.sh}")\",\"flavor\":\"$(basename "${flavor%.sh}")\"},"
                done
            done
            echo "rootfs_matrix={\"include\":[${rootfs_matrix::-1}]}" >> $GITHUB_OUTPUT

  rootfs:
    runs-on: ubuntu-22.04
    name: Build rootfs 
    needs: [config]
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.rootfs_matrix) }}
    steps:
      # ...下略，原样保留...

  build:
    runs-on: ubuntu-22.04
    needs: [rootfs, config]
    name: Build image 
    strategy:
        matrix: ${{ fromJson(needs.config.outputs.build_matrix) }}
    steps:
      - name: Get more disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug - Check current directory and files
        run: |
          pwd
          ls -la
          ls -la config/boards/

      - name: Set environment variables
        id: vars
        run: |
          source config/suites/${{ matrix.suite }}.sh
          echo "suite_version=$RELEASE_VERSION" >> $GITHUB_OUTPUT

      - name: Set DEB Asset Name
        id: deb-asset
        run: |
          DEB_ZIP_NAME="deb-packages-${{ matrix.board }}-${{ matrix.suite }}.zip"
          DEB_DOWNLOAD_URL="https://github.com/${{ env.REPO_OWNER }}/${{ env.REPO_NAME }}/releases/download/${{ env.RELEASE_TAG }}/${DEB_ZIP_NAME}"
          echo "deb_zip_name=$DEB_ZIP_NAME" >> $GITHUB_OUTPUT
          echo "deb_download_url=$DEB_DOWNLOAD_URL" >> $GITHUB_OUTPUT

      - name: Install GitHub CLI (if missing)
        run: |
          if ! command -v gh &> /dev/null; then
            sudo apt update && sudo apt install -y gh
          fi
          gh --version

      - name: Authenticate GitHub CLI
        run: echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token

      - name: Check if DEB ZIP exists in remote Release
        id: check_remote_deb
        run: |
          gh release view ${{ env.RELEASE_TAG }} \
            --repo ${{ env.REPO_OWNER }}/${{ env.REPO_NAME }} \
            --json assets \
            --jq ".assets[] | select(.name == \"${{ steps.deb-asset.outputs.deb_zip_name }}\") | .name" > /tmp/deb_asset_check.txt
          if [ -s /tmp/deb_asset_check.txt ]; then
            echo "✅ Found remote deb zip: ${{ steps.deb-asset.outputs.deb_zip_name }}"
            echo "deb_remote_exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Remote deb zip not found: ${{ steps.deb-asset.outputs.deb_zip_name }}"
            echo "deb_remote_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Download remote DEB zip (if exists)
        if: steps.check_remote_deb.outputs.deb_remote_exists == 'true'
        run: |
          mkdir -p ${{ github.workspace }}/build
          wget -O ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }} ${{ steps.deb-asset.outputs.deb_download_url }}
          ls -la ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }}
          unzip -o ${{ github.workspace }}/build/${{ steps.deb-asset.outputs.deb_zip_name }} -d ${{ github.workspace }}/build/
          ls -la ${{ github.workspace }}/build/*.deb

      - name: Install required build dependencies
        if: steps.check_remote_deb.outputs.deb_remote_exists != 'true'
        shell: bash
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential device-tree-compiler u-boot-tools

      - name: Ensure images, build, chroot, and rootfs directories exist   # ← 新增和改进
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/images
          mkdir -p ${{ github.workspace }}/build
          mkdir -p chroot/etc/default         # 保证chroot相关目录存在
          mkdir -p rootfs/dev                 # 保证rootfs相关目录存在

      - name: Build image 并忽略DTS相关错误
        shell: bash
        run: |
          set +e
          sudo $GITHUB_WORKSPACE/build.sh --board=${{ matrix.board }} --suite=${{ matrix.suite }} --flavor=${{ matrix.flavor }}
          true
        continue-on-error: true

      - name: Ensure images directory exists (pre-upload/debug)
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/images

      - name: Debug - Check image file generation
        run: |
          echo "=== Images directory contents ==="
          ls -la ${{ github.workspace }}/images/

      - name: Upload image artifact
        uses: actions/upload-artifact@v4.3.3
        with:
          name: ${{ env.ROOTFS_FILE_PREFIX }}-${{ steps.vars.outputs.suite_version }}-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}
          path: ${{ github.workspace }}/images/${{ env.ROOTFS_FILE_PREFIX }}-*-preinstalled-${{ matrix.flavor }}-${{ env.ROOTFS_ARCH }}-${{ matrix.board }}.*
          if-no-files-found: error
          compression-level: 6
          overwrite: false

      - name: Clean cache
        shell: bash
        run: sync && sudo rm -rf ${{ github.workspace }}/images/ ${{ github.workspace }}/build/ && sync
